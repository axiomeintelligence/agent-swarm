name: Publish gdrive-mcp image

on:
  push:
    branches: [main]
    paths:
      - assistant/gdrive-mcp/**

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: assistant/gdrive-mcp
          push: true
          tags: |
            ghcr.io/axiomeintelligence/gdrive-mcp:latest
            ghcr.io/axiomeintelligence/gdrive-mcp:sha-${{ github.sha }}
