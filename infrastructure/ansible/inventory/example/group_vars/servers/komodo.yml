# Komodo orchestrator vars — used by komodo.yml.
# Generate secrets with: openssl rand -hex 32

komodo_database_password: "<openssl rand -hex 32>"
komodo_jwt_secret: "<openssl rand -hex 32>"
komodo_webhook_secret: "<openssl rand -hex 32>"
komodo_admin_username: "admin"
komodo_admin_password: "<admin-password>"

# GitHub access for private repos — injected into Komodo Core as a git provider.
# Leave blank if the instance repo is public.
komodo_github_username: "<github-username>"
komodo_github_pat: "<github-personal-access-token>"

# Resource Sync bootstrap — used by tasks/resource-sync.yml
# Points Komodo at the instance repo containing agent-stacks/komodo.toml
komodo_resource_sync_name: "<client-name>-instance"
komodo_instance_repo: "<your-org>/<your-instance-repo>"
komodo_instance_branch: "main"
komodo_resource_path: "agent-stacks/komodo.toml"
komodo_git_account: "{{ komodo_github_username }}"
